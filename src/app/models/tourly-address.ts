export class TourlyAddress {
    address: string;
    phone: string;
    email: string;

    constructor(address: string, phone: string, email: string) {
        this.address = address;
        this.phone = phone;
        this.email = email;
    }
}